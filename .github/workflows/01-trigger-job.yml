name: Train model

on: workflow_dispatch

jobs: 
  train-model:
    runs-on: ubuntu-latest
    steps:
    - name: Trigger Azure Machine Learning job
      run: az ml job create --file src/model/job.yml --resource-group rg-dev-mlops --workspace-name mlw-dev-mlops
